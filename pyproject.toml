[project]
name = "autoresearch-compression"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "scipy>=1.15.3",
    "torch>=2.11.0",
    "transformers>=5.5.4",
]
